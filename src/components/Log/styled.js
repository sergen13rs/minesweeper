import { styled } from '@material-ui/core/styles';

export const LogLabelBlock = styled('div')({
  paddingBottom: '15px'
});

export const LogBlock = styled('pre')({
  width: '300px',
  height: '450px',
  overflowX: 'auto',
  lineHeight: '20px',
});